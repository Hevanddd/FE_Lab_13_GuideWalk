import { CurrentRouteComponent } from './current-route-component';
import { CurrentRouteContainer } from './current-route-container';

export const CurrentRoutePage = CurrentRouteContainer(CurrentRouteComponent);
